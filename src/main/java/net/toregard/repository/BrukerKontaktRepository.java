package net.toregard.repository;

import net.toregard.model.BrukerKontakt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrukerKontaktRepository extends JpaRepository<BrukerKontakt,String> {
}
